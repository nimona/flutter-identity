name: mochi_mobile
description: go-hash Flutter API for mobile phones
version: 0.0.2
author: Renato Athaydes <renato@athaydes.com>
homepage: https://github.com/renatoathaydes/mochi_mobile

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^3.0.0

dev_dependencies:
  json_serializable: ^3.2.3

# The following section is specific to Flutter.
flutter:
  plugin:
    # androidPackage: io.nimona.gohashmobile
    pluginClass: MochiMobilePlugin
